# 블록체인 투표 시스템 - 전체 아키텍처 및 사용자 동작 흐름

## 🏗️ 시스템 아키텍처

### 1. 3계층 구조

```
┌─────────────────────────────────────────────────────────────┐
│                     프론트엔드 (React)                         │
│  - 사용자 인터페이스 (AuthPage, RegisterPage, VotingApp)      │
│  - Web3 연동 (MetaMask)                                       │
│  - 실시간 투표 현황 표시                                       │
└────────────────────┬────────────────────────────────────────┘
                     │ Web3.js
                     ↓
┌─────────────────────────────────────────────────────────────┐
│              블록체인 레이어 (GoQuorum)                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  3개 스마트 컨트랙트 (Solidity 0.8.20)                   │ │
│  │  ├─ CitizenSBT (신원 검증)                              │ │
│  │  ├─ VotingWithSBT (투표)                                │ │
│  │  └─ VotingRewardNFT (보상)                              │ │
│  └────────────────────────────────────────────────────────┘ │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────────┐
│           네트워크 인프라 (Docker Compose)                     │
│  - 7개 Validator 노드 (QBFT/RAFT/IBFT 합의 알고리즘)          │
│  - RPC 엔드포인트: localhost:9545 (또는 10545)                │
│  - 테스트 계정 (사전 언락됨)                                   │
└─────────────────────────────────────────────────────────────┘
```

### 2. 스마트 컨트랙트 아키텍처

#### **CitizenSBT** (Soulbound Token - 신원 검증)
**목적**: 투표 자격 검증 및 1인 1투표 보장

**주요 기능**:
- 비이전(Non-transferable) ERC-721 토큰
- 신원 해시 → 지갑 주소 영구 바인딩
- 중복 등록 방지 (동일 신원, 동일 지갑)
- Verifier 주소를 통한 발급 권한 관리

**핵심 메서드**:
```solidity
mint(address to, bytes32 identityHash) // SBT 발급
hasSBT(address account) → bool         // SBT 보유 여부 확인
isIdentityRegistered(bytes32 hash) → bool  // 신원 등록 여부
```

**특징**:
- `_update()` 오버라이드로 전송 차단 (민팅/소각만 허용)
- `approve()`, `setApprovalForAll()` 비활성화
- 신원 해시는 `keccak256(name + birthdate)` 형식

---

#### **VotingWithSBT** (투표 컨트랙트)
**목적**: CitizenSBT 소유자만 투표 가능한 투표 시스템

**주요 기능**:
- 나노초 단위 정밀 투표 일정 관리 (Quorum 특성)
- 제안(Proposal) 관리 및 실시간 집계
- 투표 일정: `opensAt` → `closesAt` → `announcesAt`
- 투표 후 자동으로 RewardNFT 발급

**핵심 메서드**:
```solidity
vote(uint256 proposalId) → uint256 rewardTokenId  // 투표
canVote(address voter) → bool                      // 투표 가능 여부
getAllProposals() → Proposal[]                     // 후보 목록
ballotMetadata() → BallotMetadata                  // 투표 메타데이터
```

**투표 상태 관리**:
1. **예정** (`opensAt` 이전)
2. **진행 중** (`opensAt` ~ `closesAt`)
3. **개표 중** (`closesAt` ~ `announcesAt`)
4. **마감** (`announcesAt` 이후)

---

#### **VotingRewardNFT** (참여 보상)
**목적**: 투표 참여자에게 NFT 발급 (이전 가능)

**주요 기능**:
- 이전 가능한 ERC-721 NFT
- 투표 기록 저장 (투표자, 시간, 선택한 제안)
- Ballot별 마스코트 이미지 설정
- VotingWithSBT 컨트랙트만 발급 권한 보유

**핵심 메서드**:
```solidity
mint(address to, string ballotId, uint256 proposalId) → uint256  // NFT 발급
setMascot(string ballotId, string imageURI)  // 마스코트 설정
tokensOfOwner(address owner) → uint256[]     // 보유 NFT 목록
```

---

## 👤 사용자 동작 흐름 (End-to-End)

### **Phase 1: 신원 등록 및 SBT 발급**

```
1. 사용자 접속
   ↓
2. AuthPage (/auth)
   - 이름 입력 (예: "홍길동")
   - 본인 확인 (현재는 더미, 추후 실제 인증 연동 가능)
   ↓
3. RegisterPage (/register)
   - MetaMask 연결 요청
   - 네트워크 체크: Quorum Local (Chain ID: 0x539)
   - 지갑 주소 확인
   ↓
4. SBT 발급 프로세스
   ├─ 신원 해시 생성: keccak256("홍길동" + "2000-01-01")
   ├─ 중복 체크: checkIdentityRegistered()
   ├─ 컨트랙트 호출: CitizenSBT.mint(walletAddress, identityHash)
   └─ 트랜잭션 서명 (MetaMask)
   ↓
5. 발급 성공
   - SBT가 지갑에 영구 바인딩
   - /voting 페이지로 자동 리디렉션
```

**코드 경로**:
- `frontend/src/pages/AuthPage.tsx` → 이름 입력
- `frontend/src/pages/RegisterPage.tsx` → SBT 발급
- `frontend/src/lib/sbt.ts` → 컨트랙트 연동 로직
- `blockchain_contracts/contracts/CitizenSBT.sol`

---

### **Phase 2: 투표 참여**

```
1. VotingApp 페이지 (/voting) 진입
   ↓
2. 초기 데이터 로드
   ├─ fetchBallotMetadata() → 투표 일정, 제목, 설명
   ├─ fetchProposals() → 후보자 목록 (Alice, Bob, Charlie)
   ├─ hasVoted(walletAddress) → 투표 여부 확인
   └─ calculateTurnout() → 투표율 계산
   ↓
3. 화면 구성
   ├─ 왼쪽 사이드바:
   │   ├─ 투표 목록 (BALLOT_ID별)
   │   ├─ 내 정보 (지갑 주소, 참여 투표 수)
   │   └─ NFT 컬렉션 버튼
   │
   └─ 메인 영역:
       ├─ 투표 정보 카드 (제목, 설명, 상태)
       ├─ 투표율 게이지
       ├─ 투표 일정 (마감까지, 발표까지 카운트다운)
       └─ 후보자 카드 (이름, 공약, 투표 버튼)
   ↓
4. 투표 실행
   - 후보 카드에서 "지금 투표하기" 클릭
   - castVote(proposalId) 호출
   - VotingWithSBT.vote(proposalId) 트랜잭션
   - MetaMask 서명
   ↓
5. 투표 후 처리
   ├─ 블록체인에 투표 기록
   ├─ VotingRewardNFT 자동 발급
   ├─ hasVoted[msg.sender] = true
   └─ UI 업데이트 (버튼 비활성화: "이미 투표 완료")
```

**상태별 UI 변화**:
- **진행 중**: 투표 버튼 활성화, 결과 숨김
- **개표 중**: 투표 마감, "개표 진행 중" 배너 표시, 결과 숨김
- **마감**: 투표 마감, 최종 결과 공개, 당선자 🏆 표시

**코드 경로**:
- `frontend/src/pages/VotingApp.tsx` → 메인 투표 UI
- `frontend/src/lib/voting.ts` → 투표 컨트랙트 연동
- `blockchain_contracts/contracts/VotingWithSBT.sol`

---

### **Phase 3: NFT 수령 및 확인**

```
1. 투표 완료 후 자동 발급
   - VotingWithSBT.vote() 내부에서 rewardNFT.mint() 호출
   - NFT에 투표 정보 기록:
     ├─ voter: 투표자 주소
     ├─ timestamp: 투표 시각
     ├─ proposalId: 선택한 후보
     └─ ballotId: 투표 ID
   ↓
2. MyNFTsPage (/my-nfts) 확인
   - tokensOfOwner(walletAddress) 조회
   - NFT 카드 표시:
     ├─ 토큰 ID
     ├─ 투표 ID (Ballot ID)
     ├─ 투표 시각
     └─ 선택한 후보
```

**코드 경로**:
- `frontend/src/pages/MyNFTsPage.tsx`
- `blockchain_contracts/contracts/VotingRewardNFT.sol`

---

## 🔐 보안 메커니즘

### 1. **1인 1투표 보장**
```solidity
// CitizenSBT에서
mapping(bytes32 => address) identityToWallet;  // 신원 → 지갑
mapping(address => bool) hasSBT;               // 지갑 → SBT 보유

// VotingWithSBT에서
mapping(address => bool) hasVoted;             // 중복 투표 차단
require(citizenSBT.hasSBT(msg.sender));        // SBT 필수
require(!hasVoted[msg.sender]);                // 미투표자만
```

### 2. **비이전 SBT (Soulbound)**
```solidity
function _update(...) override {
    if (from != address(0) && to != address(0)) {
        revert TokenIsSoulbound();  // 전송 차단
    }
    // 민팅(from=0) 및 소각(to=0)만 허용
}
```

### 3. **타임스탬프 기반 투표 일정**
- Quorum의 나노초 타임스탬프 활용 (1초 = 1,000,000,000 ns)
- `opensAt` < `closesAt` < `announcesAt` 검증
- 프론트엔드에서 실시간 카운트다운

---

## 📊 데이터 흐름

```
사용자 입력 (이름)
    ↓
신원 해시 생성 (keccak256)
    ↓
CitizenSBT.mint() → 블록체인 저장
    ↓
투표 자격 획득
    ↓
VotingWithSBT.vote(proposalId) → 블록체인 저장
    ↓
VotingRewardNFT.mint() → NFT 발급
    ↓
프론트엔드 실시간 조회 (Web3.js)
```

---

## 🚀 배포 자동화

**통합 스크립트**: `blockchain_contracts/scripts/setup_and_deploy.sh`

**수행 작업**:
1. Quorum 네트워크 상태 확인 (없으면 Docker Compose 시작)
2. Node.js 의존성 설치 (`web3`, `solc`)
3. Solidity 컴파일 (solc 0.8.20, optimizer enabled)
4. 3개 컨트랙트 순차 배포:
   - CitizenSBT → VotingRewardNFT → VotingWithSBT (상호 참조)
5. 배포 정보 저장: `artifacts/sbt_deployment.json`
6. ABI 동기화: `frontend/src/abi/` 복사
7. 환경 파일 자동 생성: `frontend/.env.local`

**환경 변수 예시** (`frontend/.env.local`):
```bash
REACT_APP_RPC=http://localhost:9545
REACT_APP_CITIZEN_SBT_ADDRESS=0x1234...
REACT_APP_VOTING_CONTRACT_ADDRESS=0x5678...
REACT_APP_REWARD_NFT_ADDRESS=0xabcd...
REACT_APP_VERIFIER_ADDRESS=0xef01...
REACT_APP_CHAIN_ID=0x539
REACT_APP_CHAIN_NAME=Quorum Local
```

---

## 🎨 프론트엔드 주요 기능

### 1. **실시간 투표 현황**
- `useEffect` + `setInterval`로 1초마다 카운트다운 갱신
- Web3 이벤트 리스닝 (계정 변경, 체인 변경)

### 2. **투표 상태 자동 전환**
```typescript
function deriveBallotStatus(ballot: BallotMeta): BallotStatus {
  const now = Date.now();
  if (now < ballot.opensAt) return "예정";
  if (now > ballot.closesAt) {
    if (now < ballot.announcesAt) return "개표 중";
    return "마감";
  }
  return "진행 중";
}
```

### 3. **결과 공개 제어**
- 개표 중: 투표수 숨김 ("집계 중" 표시)
- 마감 후: 실제 투표수 공개, 최다득표자에 🏆 배지

### 4. **데모 모드 폴백**
- 네트워크 연결 실패 시 자동으로 `FALLBACK_CANDIDATES` 사용
- 실시간 데이터 없이도 UI 테스트 가능

---

## 🔧 주요 기술 스택

| 계층 | 기술 | 역할 |
|------|------|------|
| **블록체인** | GoQuorum | 프라이빗 이더리움 네트워크 (QBFT 합의) |
| **스마트 컨트랙트** | Solidity 0.8.20 | CitizenSBT, VotingWithSBT, VotingRewardNFT |
| **프론트엔드** | React 18 + TypeScript | SPA, Web3 연동 |
| **Web3 연동** | Web3.js | 트랜잭션 전송, 컨트랙트 호출 |
| **지갑** | MetaMask | 계정 관리, 트랜잭션 서명 |
| **컨테이너** | Docker Compose | 7-validator 네트워크 오케스트레이션 |
| **표준** | OpenZeppelin | ERC-721 (NFT), Ownable |

---

## 📝 핵심 설계 원칙

1. **투명성**: 모든 투표 기록이 블록체인에 영구 저장
2. **익명성**: 신원 해시만 저장, 실명은 블록체인에 미기록
3. **불변성**: SBT는 전송 불가, 투표는 수정 불가
4. **자동화**: 배포부터 환경 구성까지 스크립트로 통합
5. **사용자 경험**: 실시간 카운트다운, 투표율 게이지, 상태별 UI 변화

---

이 시스템은 **블록체인 기반 투명한 투표 + SBT 신원 검증 + NFT 보상**을 결합하여, 안전하고 공정하며 참여 동기가 높은 투표 환경을 제공합니다.
